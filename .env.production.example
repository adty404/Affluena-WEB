# Backend API base URL. No trailing slash.
# If API is reverse-proxied under the same domain, use https://your-domain.com.
# If API uses a separate subdomain, use https://api.your-domain.com.
VITE_API_BASE_URL=https://your-domain.com
